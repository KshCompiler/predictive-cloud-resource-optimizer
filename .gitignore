venv/
__pycache__/
.ipynb_checkpoints/
data/raw/*
data/processed/*
*.pt
*.joblib

# Figures: every one is regenerated by the scripts, and the PDFs already
# embed their own copies, so there is no need to keep them in the repo.
plots/*
